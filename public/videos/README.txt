Drop a short looping video named hero.mp4 into this folder and the
landing page hero + login brand panel will automatically play it as
their background (muted loop). Without it, the built-in animated
forecast backdrop renders instead. Keep the file under ~5MB; export
from Canva or use a properly licensed stock clip.
